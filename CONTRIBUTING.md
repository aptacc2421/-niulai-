# 贡献指南（Contributing Guide）

> 欢迎来贡献！**不会 PR？没关系——这份文档就是写给不会 PR 的人看的**（包括曾经的我自己）。全程照做就行。
>
> ⚠️ 本项目**全部内容由 AI 生成、未经人工审核**，维护者不对任何提交做质量保证——就是玩具，大家玩得开心就好。
> 📄 协议 **CC0 1.0 公有领域**：你贡献的东西也会一并公有化，任何人可自由转发/二创/商用，无需署名。
>
> 💡 彩蛋：如果你有 DeepSeek token 没地方花（8/17 就涨价啦！），欢迎拿来给本项目写代码、写梗、写测试——这也是消耗 token 的好去处（狗头）。

## 0. 项目是什么（30 秒版）

《直立牛平原》——2026 年电影《牛来》的粉丝二创 3D 小游戏。主角是一头生下来就站不起来的牛，在直立牛和奇奇怪怪梗生物共存的大平原上听"地摊鸡汤"，攒站立值，直到站起来。

- **想先玩玩看？** 点这个网址就行：https://aptacc2421.github.io/-niulai- （在线版，什么也不用装）
- 玩法与背景：见 [`README.md`](README.md)
- 完整策划（梗清单 / 选座购票 / 蛇追戏 / 还原原则）：见 [`直立牛平原_策划文档.md`](直立牛平原_策划文档.md)
- 怎么跑起来玩：见 [`USAGE.md`](USAGE.md)

## 1. 你能贡献什么

- **梗**：台词、语录卡、告示牌文案（改 `src/data.js`，纯数据，无代码也能改）
- **模型 / 场景**：低模方块牛、新区域、新物件
- **玩法**：新交互、小游戏（策划里的选座购票、蛇追戏都还没做！）
- **测试**：跑冒烟测试、报告 bug、补测试
- **文档**：使用说明、贡献指南、策划文档
- 想不出做什么？去策划文档里找「待办 / 预留 DLC 位」的坑

## 1.5 什么都不会？直接让 AI 替你干（AI 时代福利）🤖

不会写代码、不会用 git、不会建 PR？**都没关系**——现在是 AI 时代，你只需要把想法讲清楚。

**免费/好用的 AI 工具（2026，任选其一）**：

- **WorkBuddy**（腾讯）——网页/客户端都能用，号称"更适合国人体质的 Codex"：[入门介绍](https://www.ifanr.com/1671739)
- **Trae**（字节跳动）——免费 AI IDE，很火
- **MarsCode / 豆包**（字节跳动）——免费
- **通义灵码**（阿里）／**文心快码**（百度）——免费插件
- **DeepSeek 网页版**——免费，把下面文稿粘进去就行

**操作就三步**：
1. 复制下面的「任务文稿」，把【】里换成你的想法
2. 粘贴给任何 AI 工具，让它照着做
3. AI 建好 PR 后，你看一眼是不是你要的 → 点 Merge（或等维护者合并）→ 完事

```
你是资深开发者。请帮我对开源仓库 https://github.com/aptacc2421/-niulai- 完成一次贡献，全程自己动手，遇到问题自己解决：
1. 克隆仓库（或先 fork 再克隆你自己的）
2. 开新分支：git checkout -b feat/你的改动
3. 完成这个改动：【在这里写你想加什么，例如：在 src/data.js 里加一个新 NPC 的台词和语录卡；或者在 src/world.js 里加一个段子告示牌】
4. 本地验证：保证双击 index.html 能正常打开游玩；能跑 test/ 目录的冒烟测试更好（cd test && npm install && npm run smoke）
5. 提交并推送分支，然后创建 Pull Request，base 选 main
6. 在 PR 描述里写清楚：改了什么、怎么验证，最好附一段演示动图
7. 重要：绝对不要直接往 main 分支提交！
如果卡住了，把报错信息贴给我，我继续帮你解决。
```

> 小贴士：AI 不是万能的，遇到它反复搞不定的事（比如它改坏了界面），直接关掉重开一次对话、换个工具试试，或来仓库开 Issue 问人。

## 2. 最重要的规则：永远不要在 main 上直接改 ❗

- `main` 是"已发布"版本，**只能通过 PR 合入**
- 每做一件事，**开一个分支**，在分支上改，改完提 PR
- 分支命名：`feat/xxx`（新功能）· `fix/xxx`（修 bug）· `docs/xxx`（文档）
- 为什么？分支=你自己的草稿本，改坏了也不影响 main；PR=把草稿交上去给人 review

## 3. 第一步：把仓库弄到本地

```bash
# 方式 A：直接克隆（有写权限的话）
git clone https://github.com/aptacc2421/-niulai-.git
cd -niulai-

# 方式 B：Fork 后克隆你自己的（在 GitHub 网页点 Fork，然后克隆你自己的仓库）
git clone https://github.com/你的用户名/-niulai-.git
cd -niulai-
```

## 4. 开分支（重点中的重点！）

```bash
git checkout main        # 先回到 main
git pull                 # 拉到最新
git checkout -b feat/你的改动   # 开新分支，比如 feat/add-snake-chase
```

✅ 之后你所有的改动都在 `feat/你的改动` 上，和 main 互不干扰。

## 5. 改代码 & 本地试玩

- 改完直接**双击 `index.html`** 就能玩，不用装任何东西
- 想快速定位改哪里：看 `USAGE.md` 第八节的「文件速查表」（台词在 `src/data.js`，模型在 `src/cow.js`……）
- 可选：跑冒烟测试（见第 10 节）

## 6. 提交（commit）

```bash
git add .                       # 把改动加进来（. = 当前目录所有改动）
git commit -m "feat: 加了蛇追追逐戏"   # 写清楚你干了啥，一句话就行
```

> 小贴士：`git add .` 前可以先 `git status` 看看都有哪些改动，别把不该提交的文件（比如截图缓存）带进去。

## 7. 推送 & 建 PR（Pull Request）

```bash
git push -u origin feat/你的改动
```

然后**二选一**：

- **网页方式**：打开 GitHub 仓库页面，会出现黄色的 **"Compare & pull request"** 按钮 → 点它 → 填标题和描述 → 点 "Create pull request"
- **命令行方式**（装过 gh 的话）：
  ```bash
  gh pr create --base main --head feat/你的改动 --title "你的标题" --body "你改了什么、怎么试、附截图/动图更好"
  ```

⚠️ **base 一定选 `main`**，不要选别的分支。

**PR 描述里写什么**：改了啥（一句话到几行都行）＋ 怎么验证（"双击 index.html 走到牛棚镇就能看到新告示牌"）＋ **最好附一段演示动图**（见下节）。

### 7.5 最好附一段演示动图（Windows 也能录，很简单）

动图能让别人一眼看懂你改了啥（比如新场景、新对话、新动画），**强烈建议**但**不是必须**。Windows 小白友好方案：

- **录屏**：按 `Win + G` 打开 Xbox 游戏栏 → 点录制按钮（或 `Win + Alt + R`）→ 录 10~20 秒你玩新功能的画面 → 存成 mp4
- **mp4 转 gif（免费工具 ScreenToGif）**：下载 [ScreenToGif](https://www.screentogif.com/)（免安装版即可）→ 打开 → 「从媒体文件」选你录的 mp4 → 保存为 gif
- 不会录也没关系：PR 里写一句"我玩过了，正常"就行

## 8. 提交之后会发生什么

> ⚠️ 先说清楚：**这个项目审核很松，甚至可能不审**——它更像一个玩具，大家玩得开心就好。所以别紧张，按下面来就行：

1. PR 挂上去，等维护者看到就合（也可能很久没人看，别急）
2. **等不及的话可以自己合并**：PR 页面点 "Merge pull request" 即可（这是你自己的项目，你说了算）
3. 有意见要改 → 在分支上继续改：
   ```bash
   git add .
   git commit -m "fix: 按 review 意见调整"
   git push          # 分支一推，PR 自动更新
   ```
4. 合并后清理分支：
   ```bash
   git checkout main
   git branch -d feat/你的改动          # 删本地分支
   git push origin --delete feat/你的改动  # 删远程分支
   ```

## 9. 常见新手坑（先看这里）

| 问题 | 怎么办 |
|---|---|
| 忘了开分支就改了 | `git stash`（把改动藏起来）→ `git checkout main` → 开新分支 → `git stash pop` 把改动找回来 |
| push 被拒绝（远程有新提交） | `git pull --rebase origin main` 然后再 `git push` |
| 改坏了文件想还原 | `git checkout -- 文件名`（⚠️ 会丢掉该文件的未提交改动） |
| 不小心提交了不该提交的 | `git reset --soft HEAD~1` 撤销上一次提交（改动还在） |
| 不会用命令 | 直接在 PR 里问，或者在 Issue 里提问，别害羞 |

## 10. 测试（可选，但做了更好）

- **冒烟测试**（推荐跑）：
  ```bash
  cd test
  npm install        # 会装 puppeteer（需要 node 环境）
  npm run smoke      # 无头浏览器自动跑：加载→移动→对话→语录卡，全绿才算过
  ```
- 跑不动也没关系，手动玩一遍也算测试，PR 里说一声"我双击玩过了，正常"就行
- 完整测试套件目前**不必要**（粉丝项目、快速迭代），但别把冒烟测试跑挂了

## 11. 二创 & 合规提醒（务必读）

- 本项目是电影《牛来》的**粉丝二创，非官方、不商用**
- **不要使用官方素材**（原片画面/Logo/官方角色立绘）；新加的角色若源自商业 IP，要按策划文档 9.4 的"反和谐模式"机制做一键换皮
- A 股梗只玩"牛来/牛市"谐音，不给任何投资建议
- 图片、音效、素材注意版权：优先用程序化生成/自绘/自录

---

有问题就开 Issue 问，或者直接在 PR 里说话。祝玩得开心，牛来早日站起来 🐮
